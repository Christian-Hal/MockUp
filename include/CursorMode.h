#pragma once

// enum class that keeps track of the cursor mode
enum class CursorMode {
    Draw,
    Fill,
    Erase,
    SoftErase,
    ZoomIn,
    ZoomOut,
    Rotate,
    Pan,
    Rebind,
    ColorPick
};