
#include "ResolutionIndependence.h"

//public static float getScaleFactor(int windowWidth, int windowHeight, int baseWidth = 1920, int baseHeight = 1080) {
//	float widthScale = static_cast<float>(windowWidth) / baseWidth;
//	float heightScale = static_cast<float>(windowHeight) / baseHeight;
//	return std::min(widthScale, heightScale);
//}